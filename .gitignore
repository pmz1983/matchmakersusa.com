node_modules/
pdf/
package*.json
.env
.env.*
!.env.example
migration/.env
*.jks
*.keystore
SETUP-PROGRESS.md
SESSION-STARTERS.md
SYSTEMS-AUDIT.md

# ── Internal-only artifacts (NEVER serve publicly via GitHub Pages) ──
# Claude Code working directory: state, sessions, memory, skills, etc.
# Contains internal decisions, business context, agent doctrine.
.claude/
# Project guide for the Claude session — references owners, paths,
# session conventions. Internal.
CLAUDE.md
# Internal product/strategy docs. Default-deny posture: opt-in
# specific files by negating the rule (`!docs/whitelisted.md`) if
# any single doc is intentionally public.
docs/

# Playbook PDF — lives in 10_MARKETING/playbook-pdf/, not in public repo
playbook-pdf/
CONVERSATION-PLAN.md
AGENT_BIBLE_website.md
